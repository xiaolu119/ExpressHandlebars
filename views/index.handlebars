<div class="jumbotron">
  <h1>BURGERS App using Express Handlebars</h1>
  <h6>Welcome to My Restaurant</h6>
</div>
{{!-- <h1>Buger!</h1>
<h2>Cats that are not sleepy!</h2>

<ul>
  {{#each cats}}
    {{#unless sleepy}}
      {{> cats/cat-block sleep=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burger that are been eaten!</h2>

<ul>
  {{#each burgerdate}}
    {{#if devoured}}
   {{this.name_burger}}
    {{/if}}
  {{/each}}
</ul> --}}

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="customburger">Burger Name:</label>
    <input type="text" name="customburger">
  </div>

  <button type="submit">Add Burger</button>
</form>
